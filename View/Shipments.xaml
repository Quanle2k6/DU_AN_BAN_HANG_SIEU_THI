<UserControl x:Class="Page_Navigation_App.View.Shipments"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Page_Navigation_App.ViewModel"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1300"
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
             Style="{StaticResource Page_Style}" >
    <UserControl.DataContext>
        <vm:ShipmentVM/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Báo cáo doanh thu" FontSize="36" FontWeight="Bold"
               FontFamily="Javanese Text" Foreground="#647FBC" Grid.Row="0"/>

        <UniformGrid Columns="3" Grid.Row="1" Height="150" Margin="0,10">
            <Border Background="#FF3838" Margin="5" CornerRadius="8">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="0" FontSize="32" Foreground="White" HorizontalAlignment="Center" FontWeight="Bold"/>
                    <Label Content="Số đơn hàng trong ngày" Foreground="White" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="#C47BE4" Margin="5" CornerRadius="8">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="0" FontSize="32" Foreground="White" HorizontalAlignment="Center" FontWeight="Bold"/>
                    <Label Content="Doanh thu theo ngày" Foreground="White" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="#9CC6DB" Margin="5" CornerRadius="8">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="0" FontSize="32" Foreground="White" HorizontalAlignment="Center" FontWeight="Bold"/>
                    <Label Content="Doanh thu theo tháng" Foreground="White" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <Label Content="Xem thống kê theo tuần" FontSize="18" FontWeight="Bold" FontFamily="Javanese Text"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBox Width="180" Height="30" VerticalContentAlignment="Center"/>
                        <Button Content="Xác nhận" Width="100" Margin="10,0,0,0" Command="{Binding LoadWeeklyCommand}" Background="#FCF6D9" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>

                <lvc:CartesianChart Grid.Row="1" Series="{Binding RevenueByDaySeries}" 
                                    LegendLocation="Bottom" Margin="0,10,0,20">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Thứ" Labels="{Binding Days}">
                            <lvc:Axis.Separator>
                                <lvc:Separator Step="1" IsEnabled="False"/>
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Doanh thu (Triệu VND)" LabelFormatter="{Binding YFormatter}" MinValue="0"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>

            <Grid Grid.Column="1" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <Label Content="Xem thống kê theo quý" FontSize="18" FontWeight="Bold" FontFamily="Javanese Text"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBox Width="180" Height="30" VerticalContentAlignment="Center"/>
                        <Button Content="Xác nhận" Width="100" Margin="10,0,0,0" Command="{Binding LoadQuarterlyCommand}" Background="#FCF6D9" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>

                <lvc:CartesianChart Grid.Row="1" Series="{Binding ThongKeTheoQuy}" 
                                    LegendLocation="Bottom" Margin="0,10,0,20" Loaded="CartesianChart_Loaded_3">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Quý" Labels="{Binding Quarters}">
                            <lvc:Axis.Separator>
                                <lvc:Separator Step="1" IsEnabled="False"/>
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Doanh thu (Triệu VND)" LabelFormatter="{Binding YFormatter}" MinValue="0"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
        </Grid>
    </Grid>
</UserControl>